# DevPulse Mobile Application (React Native)

This directory contains the React Native (Expo) mobile client application.
It runs on iOS & Android devices and communicates with the backend server via REST APIs.
